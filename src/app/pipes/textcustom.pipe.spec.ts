import { TextcustomPipe } from './textcustom.pipe';

describe('TextcustomPipe', () => {
  it('create an instance', () => {
    const pipe = new TextcustomPipe();
    expect(pipe).toBeTruthy();
  });
});
